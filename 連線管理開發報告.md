# jt-ipam 連線管理（SSH / RDP / VNC）開發紀實報告

**作者：Jason Cheng (Jason Tools)**
**日期：2026-06-21 ～ 2026-06-22**
**協作：Claude Code（AI 開發助理）**

---

## 摘要

本報告依時間順序，完整記錄這次在 jt-ipam（自架、以整合為核心的 IPAM）中，從零開發出三套「瀏覽器內遠端連線管理」功能（SSH 終端機、RDP 桌面、VNC 桌面）的全過程：包含需求釐清、技術選型、概念驗證（PoC）、正式整合、過程中遭遇的每一個問題與解法，以及最終結果。

最終成果：
- **SSH 連線管理**：已完成並公開發布（v0.4.210，GitHub Apache-2.0）。
- **RDP 連線管理**：已完成、上線正式機（v0.4.211 迭代中，標示 Beta），對加固 Win11（NLA 強制）實測可用。
- **VNC 連線管理**：已完成、上線正式機（Beta），對 .154 的 TightVNC **已端到端實機驗證**；認證僅支援標準 VncAuth（不支援 MS-Logon / VeNCrypt / RealVNC RA2，見報告 §5.5）。
- 三者**完全共用同一套安全架構**（ticket→WebSocket、RBAC、加密憑證金庫、稽核），且 RDP/VNC **不增加任何必裝相依**（aardwolf 為選用、pin 有 wheel 的版本→免 Rust）。

文末並提出一個**重大發現**：以這種「人類定方向、AI 執行＋即時部署測試回饋」的模式開發大型專案，其可行性已獲得驗證。

---

## 一、專案理解（起點）

**Jason：** 「請完全了解此專案。」

**我（Claude）：** 通讀 jt-ipam 全貌 —— FastAPI + SQLAlchemy 2.0 async + PostgreSQL + Alembic + Pydantic v2 後端；Vue 3 + TypeScript + Naive UI 前端；argon2id + TOTP + JWT 認證；物件層級 RBAC（deny-by-default）；整合 DNS / LibreNMS / OPNsense / AdGuard / Wazuh / Proxmox / Ollama；systemd + apt 部署（非 Docker 為主力）。掌握部署流程（dev 在本機、rsync 到正式機 192.168.1.144）、已知地雷（前端 API 前綴、asyncpg INET/CIDR 回物件、重疊網段、長壽 SPA 舊 bundle 等）。

---

## 二、SSH 連線管理（第一套功能）

### 2.1 需求

**Jason：** 要在 IP 位址詳情頁能直接 SSH 進該主機管理。IP 編輯內可開關「啟用 SSH 連線管理」；詳情頁右上、編輯鈕左邊放一個「SSH 連線」分割按鈕（主鍵嵌入終端機、下拉另開視窗）；用 xterm.js；要結合權限。後續補充：預設不做憑證保管；要考慮 xterm.js 相依與 install/upgrade 安裝。

### 2.2 釐清（AskUserQuestion）

四項決策定案：
- 權限：**獨立的「連線管理權限」**（`users.can_ssh`），admin 或對該 IP 有寫入權者亦可。
- 認證：**密碼 + 私鑰貼上**。
- Host key：**TOFU 信任後釘選**。
- 每 IP 設定：只存 `ssh_enabled` 開關。

### 2.3 我的實作

- **後端**：migration `0081`（`ip_addresses.ssh_enabled`、`ssh_host_key`、`users.can_ssh`）、`0082`（`ssh_credentials` 加密金庫）；`permission.can_use_ssh`（deny-by-default）；`ssh_console.py`（POST `/ssh/ticket` 換單次 ticket→ WebSocket `/ssh/ws` 橋接 asyncssh，TOFU host key、keepalive、稽核開關場、絕不記憑證）；`ssh_credentials.py`（by-user 信封加密金庫：per-field 隨機 DEK + 主 KEK 包覆）。
- **前端**：`SshTerminal.vue`（xterm.js + fit addon、連線表單、心跳、狀態列）、`SshConsole.vue`（全頁）、`IPAddressEditModal` 分割按鈕、`Connections.vue`（進階→連線管理表格）。新增 npm 套件 `@xterm/xterm`、`@xterm/addon-fit`。
- **nginx**：新增 WebSocket upgrade（`map $connection_upgrade` + 專用 location）。
- **安裝**：xterm 純前端，build 即打包，install/upgrade 無額外系統套件。

### 2.4 過程中的問題與解法

| 問題 | 解法 |
|---|---|
| nginx `proxy_read_timeout duplicate`（WS location 又 include 了已設該指令的 snippet） | WS location 改成明確列 header、不 include snippet |
| ssh 遠端 build 多次跑錯目錄（`ERR_PNPM_NO_IMPORTER_MANIFEST_FOUND`） | build 一律先 `cd /opt/jt-ipam/frontend`（老雷，全程反覆踩） |
| 啟用 SSH 存檔回到詳情頁、按鈕沒出現要重整 | PATCH 回應補算 `ssh_available`（reactivity） |
| 勾「記住帳密」下次又問 | 自動選最近一筆已存帳密 |
| 卡片標題內容偏上、未垂直置中 | naive 只對 form-item label 置中；卡片標題用 `:deep(.n-card>.n-card-header)` flex 置中；已存帳密列移出 form 改 flex 列 |
| 深色主題全頁終端機底色不對、雙捲軸 | 全頁 route 自帶主題底色 + `position:fixed;inset:0;overflow:hidden` |

### 2.5 後續加強

- **憑證金庫正式版**：Jason 提供完整資安要求 spec，定案「僅核心 + 按目標/個人預設」範圍，做出 by-user 加密保管帳密/私鑰（明文絕不落 DB / 不回傳）。
- **斷線偵測**：SSH keepalive（1）+ WebSocket 心跳（2）雙保險。
- **用詞**：依台灣慣用，「詳情」→「詳細資料」（三處）。

### 2.6 結果

SSH 連線管理完成、部署正式機、**push GitHub 至 v0.4.210**（Apache-2.0 公開）。

---

## 三、RDP 概念驗證（PoC）

### 3.1 討論與排除

**Jason：** 想加 RDP，先討論。明確排除：**guacd、Java/.NET/WASM 用戶端、產 .rdp 喚起原生 client**。請評估 aardwolf / grdp（並順帶比較 guacd 缺點）。

**我：** 分析 guacd（成熟但重相依：要跑 guacd daemon + libguac + 協定庫）；aardwolf（純 Python 生態、純瀏覽器可行）；grdp（Go）。

### 3.2 實機 PoC（目標：Win11 192.168.1.154，NLA 強制）

**Jason：** 「rdp 請你開始 poc，guacd 不要，請測 aardwolf/grdp，目標 192.168.1.154，全部測完再給我結論。」

過程與發現：
- **環境確認**：.154 為加固 Win11，`UserAuthentication=1`（NLA 強制）—— 這是兩個 library 的生死題。
- **帳號更正**：我一開始用錯帳號，Jason 更正為 `user` / `test!@#$`。
- **grdp**：在 Linux **連編譯都過不了**（`plugin/cliprdr` 是 Windows-only 卻被核心 MCS 層引用，且內部有 `GetFileInfo` 回傳值數量不一致的 bug）→ 要 fork 修補才能在我們的 Linux 後端跑 → **出局**。
- **aardwolf**：✅ **NLA 連線成功（2 秒）**、擷取到 framebuffer（1280×800 PNG）、誘發活動下 ~198 update-rect/s ~3MB/s 解碼。⚠️ 但 0.2.14（latest）只有 sdist、**build 需要 Rust**（其 Rust 擴充 `librlers` 做 RLE 解壓）。

### 3.3 端到端 PoC（aiohttp + canvas）

我寫了一支 ~80 行的 aiohttp + aardwolf 橋接伺服器（PNG tile→WebSocket→canvas），Jason 用瀏覽器實際操作 .154（逛 Yahoo）。隨後一輪輪回饋與修正：

| 回饋 | 根因 / 解法 |
|---|---|
| 游標不見了（含本地） | `cursor:none` + 遠端游標未渲染 → 改用本地游標（座標 1:1） |
| 畫面鋸齒 | 移除 `image-rendering:pixelated`、bpp 提到 24 |
| hover 看不到 / 位置不同步 | 滑鼠移動原本誤送成 button-release，改用 `MOUSEBUTTON_HOVER`（PTRFLAGS.MOVE） |
| 滾輪一滾就卡死 | 追查發現**真因不是滾輪**，是殘留的 Windows 工作階段把連線 reset；但也修好了 aardwolf 兩個 wheel bug |
| 右鍵跳出瀏覽器選單 | `contextmenu` preventDefault |

**aardwolf 的兩個 wheel bug（重要）**：
1. `WHEEL_DOWN` 只設了 `WHEEL_NEGATIVE` 卻**漏設 `WHEEL` flag** → 伺服器根本不當它是滾輪事件 → 向下捲完全無效。
2. wheel 經佇列時 `steps` 恆為 0 → 即使向上也幾乎不捲。
修正後：5 次滾輪後串流 915 個 tile（真的有捲動）、連線存活。

### 3.4 多 user / 多 session 與剪貼簿討論

- **多 user**：不同目標各自獨立、互不衝突；但瓶頸是 **Python 單核（GIL）編解碼** → 多條同時忙碌會擠一顆核，要多行程才能擴展；同一目標受 Windows 用戶端「單一 session」限制（會互踢）；殘留的「已中斷連線」session 會 wedge 後續連線（我們踩了數次，要 SSH `logoff` 清）。
- **剪貼簿**：aardwolf 架構上支援文字剪貼簿，但 PoC 關著；Jason 決定「先不做，之後做文字的就好」。
- **遠端游標**：aardwolf 預設丟棄指標更新（且其 COLOR pointer 解析有 mask 長度讀反的 bug）→ Jason 決定「先不用，維持現狀」。

### 3.5 PoC 結論

aardwolf **單人 in-browser RDP 對加固 Win11 可行已證實**（NLA/檢視/點擊/打字/滾輪皆可），但屬研究級 library，每往生產級推一步就要修一個它的 bug 或補一塊架構。

---

## 四、RDP 正式整合進 jt-ipam

**Jason：** 「rdp poc 先到這，可以開始做進去 jt-ipam，然後要注意相依性、注意 install upgrade。」

### 4.1 規劃（plan mode）＋相依關鍵發現

- **相依破解**：aardwolf **0.2.13 有 cp311/312/313 的 manylinux wheel**（內含已編譯的 `librlers`）→ **pin 0.2.13 即可免 Rust**。正式機是 python3.12 → 直接吃 wheel。先前 Rust 之痛純粹是抓到 latest 0.2.14（sdist-only）。
- **決策（AskUserQuestion）**：
  - 安裝＝**best-effort 自動嘗試**裝 `[rdp]` extra（`--only-binary=:all:`，沒 wheel 就快速失敗、不觸發 Rust、不中斷主安裝）。
  - 資料模型＝**平行加 `rdp_*`**（不動已上線的 SSH）。

### 4.2 實作

- **後端**：migration `0083`（`ip_addresses.rdp_enabled` + 金庫 `protocol`/`domain`）；`can_use_rdp`；`rdp_console.py`（ticket→WS、NLA `rdp+ntlm-pwb64://`、並發上限、稽核）；`pyproject` 加 `[rdp]` extra（pin `aardwolf==0.2.13`）；`RDP_AVAILABLE` graceful detect（未裝則端點 503、前端隱藏）。
- **避開 aardwolf bug 不 fork**：輸入直接呼叫 `conn.send_mouse`/`send_key_*`；滾輪一律用 `WHEEL_UP` 並把方向的 sign 位放進 `steps` → 同時繞過「WHEEL_DOWN 漏 flag」與「steps=0」兩個 bug。**前端零新增套件**（RDP 用原生 canvas）。
- **前端**：`RdpScreen.vue`、`RdpConsole.vue`、三入口（編輯開關、詳情分割鈕、連線管理頁）。
- **install/upgrade**：`jt-ipam.sh` 兩條路徑都加 best-effort `[rdp]` 安裝。

### 4.3 過程中的問題與解法

| 問題 | 解法 |
|---|---|
| **migration 上不去**（`value too long for character varying(32)`） | revision id `0083_ip_rdp_and_credential_protocol`（35 字）超過 `alembic_version` 的 VARCHAR(32) → 改名 `0083_ip_rdp_cred_protocol`（25 字），刪正式機舊檔重跑 |
| **WebSocket 連線失敗**（透過 ipam.jason.tools） | 正式機 nginx 只有 SSH 的 WS location、沒有 RDP 的 → RDP WS 落到一般 `/api/` 沒帶 Upgrade header → 把 location regex 拓寬成 `(ssh\|rdp)/ws` 並 reload |
| 連線管理頁 OS 欄與詳情頁不一致 | list 端點沒套來源優先序 → 比照 `get_address` 套 `effective_os`（librenms→wazuh→scanner） |
| 稽核 object_type 對 RDP 帳密仍是 `ssh_credential` | 改 `f"{protocol}_credential"` → `rdp_credential` |
| 稽核「目標」欄顯示 UUID 截斷 | `_LABEL_REGISTRY` 加憑證類型 → 顯示 label |
| 稽核 switch_port 顯示 `switch-003 / eth1/0/8` | 比照其他頁改 `switch-003@eth1/0/8`（@ 格式） |
| pkill 反覆「無輸出 exit 1」 | 發現 `pkill -f 'rdp-poc/server.py'` 會**比對到自己的 shell 命令列把自己殺掉** → 改用「依埠號找 PID」殺 |
| 解析度下拉截斷 / 預設要 auto / auto 仍有捲軸 | `consistent-menu-width=false`；預設改 `auto`；auto 改**量測實際 canvas 容器**尺寸→無捲軸 |

### 4.4 Beta 標示與結果

依 Jason 要求，RDP 全面標 **Beta**（開關、連線畫面、工具列、按鈕旁、篩選）。部署正式機 **v0.4.211**（未 push）。對加固 Win11 .154 透過 ipam.jason.tools 實測：**桌面顯示、點擊、打字、滾輪皆正常**。

---

## 五、VNC 連線管理（依 ssh/rdp 模式）

**Jason：** 「接著依 ssh rdp 模式 規劃 vnc 連線管理功能。」

### 5.1 規劃關鍵發現

aardwolf **本身就支援 VNC**（`VNCConnection`，介面與 RDP 完全相同）→ **幾乎完全複用 RDP 橋接、不需新增任何相依**。差異只在：`vnc://` scheme、密碼認證（無帳號）、桌面尺寸由**伺服器**決定。

### 5.2 實作

- **後端**：migration `0084`（`ip_addresses.vnc_enabled`）；`can_use_vnc`；`vnc_console.py`（ticket→WS、`vnc+plain-password://`、稽核）；金庫 `protocol='vnc'`；`/me` 加 `vnc_supported`。
- **前端**：`VncScreen.vue`（密碼+連接埠，canvas 依 server framebuffer 尺寸）、`VncConsole.vue`、三入口、Beta。
- **nginx/install**：WS regex 拓寬成 `(ssh\|rdp\|vnc)/ws`（正式機 reload + 範本 + `jt-ipam.sh` 升級 patch，且會自動拓寬舊 install）。

### 5.3 過程中的問題與解法

| 問題 | 解法 |
|---|---|
| aardwolf 的 VNC `send_mouse` 是**壞的**（按鍵 mask 反向、無滾輪、無 steps 參數） | import 時 monkeypatch 一個正確的 RFB PointerEvent（累積 button mask、滾輪用 button 4/5 一次按放） |
| aardwolf 的 VNC `send_key_char` 也**壞的**（把字元字串塞進 `pack(...I)` → `struct.error`） | VNC 鍵盤要用 **X11 keysym**（非 PC scancode）→ monkeypatch：字元 ord→keysym、特殊鍵對應 X11 keysym |
| 憑證清單 `protocol=vnc` 過濾沒生效（白名單漏 `vnc`）→ VNC 畫面誤顯示 RDP 帳密 | 白名單補 `vnc`（且 VNC 連線後端本就會擋 `protocol!=vnc`，不會真拿錯密鑰連） |
| VNC 沒有「用戶端選解析度」概念 | 桌面尺寸 connect 後讀 `conn.width/height`（伺服器決定），於 connected 狀態回傳給前端設定 canvas；右上加「自動縮放／原始解析度」切換（縮放時滑鼠座標換算） |
| 金庫 username 必填、但 VNC 無帳號 | 前端帶固定 `username="vnc"`（不用於認證） |

### 5.4 結果與實機驗證（已完成）

起初 LAN 上沒有標準 VNC 伺服器（掃 192.168.1.0/24，只有 3 台 IPMI BMC 開 5900、但不送 RFB greeting＝非標準 VNC；ud-22 沒開 VNC 埠）。於是依 Jason 指示，**在 Win11（.154）以 SSH 靜默安裝 TightVNC**（服務模式、5900、VNC 密碼）做為測試標的：

- RFB greeting `RFB 003.008`（標準 VNC）。
- aardwolf 端到端：連線 OK（1280×1024）、滑鼠（patch 後）OK、**鍵盤字元＋特殊鍵（patch 後）OK**、滾輪 OK、串流近千 frames。
- → **VNC 已端到端驗證可用**。瀏覽器實測：桌面顯示、點擊、打字、滾輪、自動縮放皆正常。

### 5.5 VNC 認證支援範圍與限制（roadmap）

aardwolf 的 VNC **只實作 RFB security type 1（None）與 type 2（VncAuth，純密碼）**（原始碼 `vncconnection.py` 明寫 only support these 2）。因此：

| VNC 伺服器 / 認證 | 支援？ |
|---|---|
| TightVNC（VncAuth 密碼） | ✅ 已實測 |
| UltraVNC **一般 VNC 密碼**（password-only） | ✅ 同 type 2，可用 |
| TigerVNC / x11vnc（VncAuth） | ✅ 可用 |
| **UltraVNC MS-Logon（帳號+密碼）** | ❌ 未支援（不同 security type；連線會在認證協商階段中止） |
| **VeNCrypt（帳號型）** | ❌ 未支援 |
| **RealVNC RA2 / RA2ne（RSA-AES，預設）** | ❌ 未支援（除非該 server 改設 legacy VncAuth） |

**決策**：MS-Logon **先不實作**（小眾、且 MS-Logon I 本身不安全；估 1.5–2.5 個工作天且 crypto 線路格式風險高）。VNC 連線畫面（輸入密碼處）已**標示**：「僅支援標準 VNC 密碼認證（VncAuth）；不支援 UltraVNC MS-Logon 等帳號型認證。」之後若有客戶明確需要 MS-Logon-only 目標，再投入。

### 5.6 收尾（測試標的）

TightVNC 是為測試而裝；測畢可 `msiexec /x` 靜默移除（或保留供後續驗證）。

---

## 六、其他貫穿全程的事項

- **版本策略**：Jason 指示「下個版本號起推進到 0.5」。目前正式機跑 0.4.211（RDP/VNC 迭代中），待全部驗收後 bump **0.5.0** 並走 GitHub push SOP。
- **PoC 收尾**：無認證的 PoC server（dev box:8900）整合完成後已拆除。
- **安全一致性**：SSH/RDP/VNC 三者共用 ticket（單次 60s、綁 user×ip）、WS 兩處重查權限、目標 host 鎖死該 IP（防 SSRF）、憑證信封加密只在連線當下解密、稽核開關場不記憑證、並發上限。

---

## 七、目前狀態總表

| 功能 | 狀態 | 版本 | 實機驗證 |
|---|---|---|---|
| SSH 連線管理 | ✅ 完成、已公開 | v0.4.210（GitHub） | ✅ 已驗 |
| RDP 連線管理 | ✅ 完成、上線（Beta） | v0.4.211（未 push） | ✅ 對 Win11 .154 已驗 |
| VNC 連線管理 | ✅ 完成、上線（Beta） | v0.4.211（未 push） | ✅ 對 .154 TightVNC 已驗 |

VNC 認證限制：支援 None / VncAuth；**不支援** MS-Logon、VeNCrypt、RealVNC RA2（見 §5.5）。

待辦：驗收後 bump **0.5.0** + push GitHub（含 SSH 修正、RDP、VNC、Beta、稽核修正、nginx/install 同步）。

---

## 八、重大發現：以此模式開發大型專案的可行性

這次最值得記錄的，不只是三套連線功能本身，而是**開發模式本身得到了驗證**：

- **人類定方向、AI 執行、即時部署測試回饋**的協作模式，能在一個**真實、龐大、已上線**的專案（jt-ipam）上，於數小時內完成「需求釐清 → 技術選型 → 概念驗證 → 正式整合 → 部署 → 一輪輪 UI/bug 修正 → 文件」的完整循環。
- AI 能**自行做概念驗證**（架 PoC、對真實加固主機實測、比較 aardwolf vs grdp 並下結論）、**追到第三方 library 的底層 bug**（aardwolf wheel flag、VNC 滑鼠 mask、grdp Windows-only）、並用**最低風險的方式繞過**（呼叫端 workaround、pin 有 wheel 的版本、runtime monkeypatch 而非 fork）。
- 對**生產級非功能需求**（相依管理、install/upgrade 不破壞既有客戶、RBAC、加密、稽核、nginx WebSocket、版本與發布 SOP）能一併納入，而非只寫「能跑」的程式。
- 人類的角色高度槓桿化：主要是**定方向、做決策（AskUserQuestion）、用瀏覽器實測回饋**；繁瑣的實作、除錯、部署、跨檔一致性由 AI 承擔。

**結論：透過這種方式開發大型專案的可行性，已經得到驗證。** 關鍵成功因素是「可即時部署到真實環境並由人類實測回饋」的緊密迴圈 —— 它讓 AI 的產出能被快速證偽與修正，而不是停留在「看起來對」的程式碼。

---

*本報告由 Claude Code 依完整對話紀錄整理；作者 Jason Cheng (Jason Tools)。*
